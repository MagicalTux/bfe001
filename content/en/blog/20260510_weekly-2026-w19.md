---
title: "Weekly open source: goro's VM lands, OxideAV fills the codec matrix"
slug: "weekly-2026-w19"
date: 2026-05-10
description: "Bytecode VM ships in goro. libwallet fixes a Bitcoin bug that broke every send. OxideAV widens from a document stack into a full codec matrix, including hardware-acceleration crates and a 3D-asset stack."
---

The thing W18 said was coming — "a VM lands in goro" — landed.
libwallet shipped a single fix that had been silently breaking every
Bitcoin send. OxideAV widened from "the document stack and a handful
of codecs" into something much closer to a full ffmpeg-shaped matrix
in pure Rust.

## goro: bytecode VM

[**goro**](https://github.com/KarpelesLab/goro) (PHP in Go) had been
running on a tree-walking interpreter and an ahead-of-time PHP → Go
transpiler. The missing middle was a bytecode VM. This week it
landed.

The shape:

- **Compiler + runtime skeleton** — lower the AST to bytecode,
  dispatch through a small VM, `GORO_VM` gate so the existing
  interp stays the default while coverage settles.
- **Native ops** — literals, variables, binops (routed through
  `compiler.EvalBinop` for full PHP semantics), arrays + indexing +
  append, `foreach`, `??` null coalesce, object instantiation +
  property read + method call, `throw` / `try` / `catch` /
  `finally`, closure bodies dispatched through the VM with `$this`
  binding, native string interpolation, class const / static /
  `::class` access.
- **Wins** — slot-array locals (skip the hashtable on reads) →
  −50% on the Arithmetic micro-bench. Inline closures with
  indirect calls → −21% on FunctionCalls.

Regression-free against the phpt suite, with diff tests comparing
interp vs. VM. The VM is opt-in for now; the default flips later.

## libwallet: a double-reversed txid

[**libwallet**](https://github.com/KarpelesLab/libwallet) v0.4.13 →
v0.4.17. The headline fix: `wlttx/bitcoin: fix double txid
byte-reversal that broke every send`. A txid endian had been flipped
twice, so every signed Bitcoin transfer referenced inputs that
didn't exist on-chain. The kind of bug that only surfaces when you
actually try to broadcast.

Also this week:

- `priority` field on Bitcoin transfers selects cheap vs. fast fee
  budget.
- `wlttx/maxSendable` pins inputs + fee rate so the "send max"
  amount survives between estimate and broadcast, and accounts for
  both `m/0` and `m/1` derivation chains with per-input vsize.
- UTXO fetcher collapsed to a single source (`modchain_assets`) so
  coin selection and broadcast can't disagree about what's
  available.
- Bitcoin broadcast errors now include inputs + raw hex.
- Dart FFI loads the right native library per platform.

## OxideAV: the matrix fills out

W18 covered the document stack — TTF / OTF / SVG / PDF — plus the
first lossless and legacy codec sweeps. W19 is the matrix expanding
sideways into video, audio, images, hardware, and 3D.

### Encoders pushed deep

- **H.266 / VVC**
  ([oxideav-h266](https://github.com/OxideAV/oxideav-h266)) —
  encoder rounds 49 → 56 over the week: chroma SAO RDO + apply,
  chroma residual emit, joint `alf_luma_clip_idx` coeff/clip RDO
  with chroma SAO merge, explicit `tu_*_coded_flag` CABAC, MTT
  BT/TT split syntax, per-CU `cu_qp_delta`, forced QT split for
  128×128 CTBs, multi-row CU neighbour tracking.
- **JPEG XL**
  ([oxideav-jpegxl](https://github.com/OxideAV/oxideav-jpegxl)) —
  re-implementing against ISO/IEC 18181-1:2024. All five small
  lossless fixtures decode pixel-correct; rounds 20 → 28 worked
  through DC_GROUP boundary recount, ANS-final-state oracle,
  parent-dispatch IDCT, and Annex L colour transforms (XYB / YCbCr
  inverse).
- **WebP VP8L**
  ([oxideav-webp](https://github.com/OxideAV/oxideav-webp)) —
  Viterbi-style optimal LZ77 for the lossless path; VP8
  intra-frame holds at 100% bit-exact.
- **MagicYUV**
  ([oxideav-magicyuv](https://github.com/OxideAV/oxideav-magicyuv))
  — clean-room rebuild rounds 1-3, all 17 FOURCCs plus encoder,
  declared 100% with Auditor sign-off.
- **TTA**
  ([oxideav-tta](https://github.com/OxideAV/oxideav-tta)) —
  clean-room rebuild rounds 1+2, Auditor PASS.

### Codec siblings, new this week

**Video** —
[H.261](https://github.com/OxideAV/oxideav-h261),
[H.263](https://github.com/OxideAV/oxideav-h263),
[H.264](https://github.com/OxideAV/oxideav-h264),
[H.265](https://github.com/OxideAV/oxideav-h265),
[AV1](https://github.com/OxideAV/oxideav-av1),
[VP6](https://github.com/OxideAV/oxideav-vp6) /
[VP8](https://github.com/OxideAV/oxideav-vp8) /
[VP9](https://github.com/OxideAV/oxideav-vp9),
[MPEG-1/2](https://github.com/OxideAV/oxideav-mpeg12video) /
[MPEG-4](https://github.com/OxideAV/oxideav-mpeg4video) /
[MS-MPEG4](https://github.com/OxideAV/oxideav-msmpeg4),
[MJPEG](https://github.com/OxideAV/oxideav-mjpeg),
[Dirac](https://github.com/OxideAV/oxideav-dirac),
[Theora](https://github.com/OxideAV/oxideav-theora),
[ProRes](https://github.com/OxideAV/oxideav-prores),
[AVIF](https://github.com/OxideAV/oxideav-avif).

**Audio** —
[AAC](https://github.com/OxideAV/oxideav-aac),
[AC-3](https://github.com/OxideAV/oxideav-ac3),
[AC-4](https://github.com/OxideAV/oxideav-ac4),
[Opus](https://github.com/OxideAV/oxideav-opus) +
[CELT](https://github.com/OxideAV/oxideav-celt),
[Vorbis](https://github.com/OxideAV/oxideav-vorbis),
[FLAC](https://github.com/OxideAV/oxideav-flac),
[MP1](https://github.com/OxideAV/oxideav-mp1) /
[MP2](https://github.com/OxideAV/oxideav-mp2) /
[MP3](https://github.com/OxideAV/oxideav-mp3),
[Speex](https://github.com/OxideAV/oxideav-speex),
[GSM](https://github.com/OxideAV/oxideav-gsm),
[iLBC](https://github.com/OxideAV/oxideav-ilbc),
[G.711](https://github.com/OxideAV/oxideav-g711) /
[G.722](https://github.com/OxideAV/oxideav-g722) /
[G.723.1](https://github.com/OxideAV/oxideav-g7231) /
[G.728](https://github.com/OxideAV/oxideav-g728) /
[G.729](https://github.com/OxideAV/oxideav-g729),
[Ogg](https://github.com/OxideAV/oxideav-ogg) container.

**Image** —
[PNG](https://github.com/OxideAV/oxideav-png),
[JPEG 2000](https://github.com/OxideAV/oxideav-jpeg2000),
[OpenEXR](https://github.com/OxideAV/oxideav-openexr),
[Radiance HDR](https://github.com/OxideAV/oxideav-hdr),
[BMP](https://github.com/OxideAV/oxideav-bmp),
[ICO](https://github.com/OxideAV/oxideav-ico),
[TGA](https://github.com/OxideAV/oxideav-tga),
[PCX](https://github.com/OxideAV/oxideav-pcx),
[QOI](https://github.com/OxideAV/oxideav-qoi),
[DDS](https://github.com/OxideAV/oxideav-dds),
[Farbfeld](https://github.com/OxideAV/oxideav-farbfeld),
[WBMP](https://github.com/OxideAV/oxideav-wbmp),
[Apple PICT](https://github.com/OxideAV/oxideav-pict),
[Amiga IFF/ILBM](https://github.com/OxideAV/oxideav-iff),
[ICER](https://github.com/OxideAV/oxideav-icer) (NASA Mars rover
compression).

**Tracker / chip** —
[Amiga MOD](https://github.com/OxideAV/oxideav-mod),
[ScreamTracker S3M](https://github.com/OxideAV/oxideav-s3m),
[Nintendo NSF](https://github.com/OxideAV/oxideav-nsf).

### Hardware acceleration

A new family of sibling crates wraps OS hardware codec engines
behind the same oxideav-core API:

- [VA-API](https://github.com/OxideAV/oxideav-vaapi) — Linux / Intel
- [VDPAU](https://github.com/OxideAV/oxideav-vdpau) — NVIDIA on Linux
- [Vulkan Video](https://github.com/OxideAV/oxideav-vulkan-video) — cross-vendor GPU
- [NVIDIA NVENC/NVDEC](https://github.com/OxideAV/oxideav-nvidia)
- [VideoToolbox](https://github.com/OxideAV/oxideav-videotoolbox) — Apple video
- [AudioToolbox](https://github.com/OxideAV/oxideav-audiotoolbox) — Apple audio

These are the only crates in the project that use OS FFI, and
they have to, because the engines they wrap are OS-provided. The
workspace README was reframed to make that explicit: "pure Rust" is
a preset (`pure-rust` = all minus hwaccel), not a project-wide
claim.

### PDF: encryption

[**oxideav-pdf**](https://github.com/OxideAV/oxideav-pdf) landed
ISO 32000-1 §7.6 encryption decode, including AES-256 revision 5
and revision 6. Password-protected PDFs round-trip.

### 3D assets

A 3D-mesh stack plumbed through the scene model:
[mesh3d](https://github.com/OxideAV/oxideav-mesh3d) as the
in-memory shape, with
[STL](https://github.com/OxideAV/oxideav-stl),
[OBJ](https://github.com/OxideAV/oxideav-obj),
[glTF](https://github.com/OxideAV/oxideav-gltf), and
[USDZ](https://github.com/OxideAV/oxideav-usdz) siblings. The USDZ
test path runs through Apple's `usdzconvert` as an oracle (CI pins
Python 3.9 + `usd-core` 21.11 for compat).

### Workspace shape

- `oxideav-format-all` renamed to
  [**oxideav-meta**](https://github.com/OxideAV/oxideav-meta).
  Explicit `register_all(ctx)` instead of `linkme`, with Cargo
  features to shape the registered set. The
  [`oxideav`](https://github.com/OxideAV/oxideav) umbrella was
  thinned out: every sibling exposes a `register(ctx)` via an
  `enable!()` macro, and the umbrella stops depending on
  everything.
- Pure-Rust preset (`pure-rust` = all minus hwaccel) is now
  first-class.
- New CLI surface: `oxideav info <codec>` prints backend
  availability; `bench` measures per-codec throughput against
  hardware backends; `transcode` handles multi-stream inputs;
  `list` shows the per-codec backend table grouped by media type.
- [oxideav-meta](https://github.com/OxideAV/oxideav-meta) and
  [oxideav-cli-convert](https://github.com/OxideAV/oxideav-cli-convert)
  each got their first 0.0.x / 0.1 cuts.

## Also this week

- **[oxideav-vfw](https://github.com/OxideAV/oxideav-vfw)** —
  Video for Windows / DirectShow shim, working through
  `IBaseFilter` / `IFilterGraph` / `IPin::ReceiveConnection`
  wiring, then auto-discovery.
- **[oxideav-image-filter](https://github.com/OxideAV/oxideav-image-filter)**
  — image filter graph, rounds 7 → 10.

## Next week

JPEG XL keeps grinding through the 2024 spec. H.266 continues the
encoder loop. The hardware-acceleration crates start sprouting
first working backends. goro's VM begins flipping on more PHP
semantics.
